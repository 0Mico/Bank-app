package com.account.factory;

import com.account.entity.Account;
import com.common.dto.AccountDTO;
import com.common.interfaces.EntityFactory;

public interface AccountFactory extends EntityFactory<Account, AccountDTO> {
}
